import { DocumentsManager } from './DocumentsManager'

// Keep the application route stable while rendering the codebook-compliant vault.
export function DocumentsPage() {
  return <DocumentsManager />
}
